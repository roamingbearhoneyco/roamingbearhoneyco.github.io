# Site URL (for Stripe redirect after checkout)
SITE_URL=http://localhost:3000

# ⚠️  STRIPE SECRETS - DO NOT PUT IN THIS FILE ⚠️
# These go to Supabase Vault ONLY via CLI:
#   supabase secrets set STRIPE_SECRET_KEY=sk_test_YOUR_KEY
#   supabase secrets set STRIPE_WEBHOOK_SECRET=whsec_YOUR_SECRET
#
# Note: STRIPE_PUBLISHABLE_KEY is not needed for our setup
# (we use Stripe-hosted checkout, not client-side form)
